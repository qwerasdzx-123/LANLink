<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kalaspace.lanlink"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- 网络通信基础权限（Fyne 默认模板也有，但自定义 manifest 需自行声明） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 允许应用接收 239.x 组播包（配合下面的 MulticastLock） -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!-- Android 12+/API32+ 局域网近场设备发现（无需定位权限） -->
    <uses-permission android:name="android.permission.NEARBY_DEVICES" />
    <!-- 兼容老版本文件访问（Fyne 默认模板包含） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!-- 注意：uses-sdk 与 android:icon 由 Fyne 自动合成，不能在此手写 -->
    <application
        android:label="LANLink"
        android:debuggable="true">

        <activity
            android:name="org.golang.app.GoNativeActivity"
            android:label="LANLink"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:exported="true"
            android:theme="@android:style/Theme"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="android.app.lib_name" android:value="LANLink" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
